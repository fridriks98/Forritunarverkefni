#Create a program that takes 2 strings as input. If the strings are \
#the same length the program should print “The strings are the same length”. \
#If they are not the same length the program shouldn’t print anything.
string1 = input("Enter a string: ")
string2 = input("Enter another string: ")

if len(string1) == len(string2):
    print("The strings are the same lenght")
    
